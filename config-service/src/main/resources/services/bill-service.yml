spring:
  jpa:
    database: MYSQL
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: create
    properties:
  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:5432/bill_service_database
    username: root
    password: root
    driver-class-name: org.mysql.cj.jdbc.Driver

server:
  servlet:
    context-path: /bills
  port: 8082